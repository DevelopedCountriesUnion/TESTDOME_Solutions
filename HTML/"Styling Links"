<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Styling links</title>
    <style type="text/css">
      a{
        
        text-transform: uppercase; /* uppercase */
        text-decoration: none; /* no underline */
        cursor: help; /* mouse pointer be a question mark */
        color: purple;  
      } 
      
      a::after { content: "<"; }   /* The ::after selector inserts something after the content of each selected element(s).  */
      a::before { content: ">"; }  /* The ::before selector inserts something before the content of each selected element(s). */
    </style>
  </head>
  <body>
    <a href="http://www.testdome.com"> Check documentation </a>
  </body>
</html>
