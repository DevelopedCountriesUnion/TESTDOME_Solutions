function ensure(value) {
  if(value === undefined) {     <!-- ===	equal value and equal type -->
    throw new Error('no arguments');
  }

  return value;
}

try {
  console.log(ensure());
} catch(err) {
  console.log(err);
}
